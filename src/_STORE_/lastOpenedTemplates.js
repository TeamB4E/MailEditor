import * as CONSTANTS from '_constants';
import _ from 'lodash';

const _defaultState = [];

const lastOpenedTemplates = (state = _defaultState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default lastOpenedTemplates;
